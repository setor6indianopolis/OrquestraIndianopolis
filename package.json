{
  "name": "orquestra-app",
  "version": "1.0.0",
  "private": true,
  "description": "Orquestra — Sistema de Gestão Musical",
  "scripts": {
    "dev": "npx serve public",
    "build": "echo 'Static site — no build needed'"
  }
}
